'use client';

import type { Dataset } from '@/lib/api';

export default function DatasetSidebar(_props: {
  dataset: Dataset;
  onClose: () => void;
  onWidthChange?: (w: number) => void;
}) {
  return null;
}
