'use client';

import type { Job } from '@/lib/types';

export default function SecondarySidebar(_props: {
  job: Job;
  onClose: () => void;
  onWidthChange?: (w: number) => void;
}) {
  return null;
}
