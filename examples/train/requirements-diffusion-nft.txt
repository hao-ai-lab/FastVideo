# Reward runtime pins for DiffusionNFT/GenRL video reward training.
#
# The vendored HPSv3/VideoAlign runtimes follow the Qwen2-VL package layout
# used by Transformers 4.x. Newer Transformers 5.x moves Qwen2VL internals and
# can load VideoReward with missing language-model weights.
datasets==3.6.0
fire>=0.7.0
matplotlib==3.10.3
peft==0.19.1
qwen-vl-utils==0.0.11
safetensors==0.5.3
timm==1.0.15
transformers==4.57.3
trl==0.8.6
